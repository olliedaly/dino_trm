# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
references/
/data/

# Model checkpoints (8.9G, regenerable from the training recipe)
checkpoints/

# Hydra per-run output dirs (logs/configs duplicated under results/)
outputs/

# Intermediate training-progress mask snapshots (keep only *_final.png)
results/**/masks_step*.png

# Transient COCO download / training progress logs
results/coco_download.log
results/coco_train.log
results/coco_controls_logs/

# Local-only handoff/results docs (kept on disk, not tracked)
RESULTS.md
NEXT_STEPS.md
